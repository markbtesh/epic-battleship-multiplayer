{
  "builds": [
    {
      "src": "battleship-multiplayer/angular.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "battleship-multiplayer/dist/battleship-multiplayer" // Ensure this matches the `outputPath`
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
